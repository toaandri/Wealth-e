# Assumptions

- The repository remains a single repository named `wealth-e`.
- Existing user changes, including the existing `Readme.md`, are preserved.
- Empty architecture areas are represented with `.gitkeep` files so Git tracks them.
- Technology versions and runtime configuration will be selected during P0/P1, not invented in the scaffold.
- Release tags are created only after their documented gates and explicit authorization.

